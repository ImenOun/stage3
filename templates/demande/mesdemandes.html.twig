{% extends 'baseF.html.twig' %}

{% block body %}
    <div class="row mb-4">
        <div class="col-12">
            <h1>Mes Attestations</h1>
            <div class="card">
                <div class="card-body">
                    {% if demandes is empty %}
                        <div class="alert alert-info mt-3">Aucun attestation trouvé.</div>
                    {% else %}
                        <table class="table table-striped table-bordered mt-3">
                            <thead class="bg-primary text-white">
                                <tr>
                                    <th>Type</th>
                                    <th>Statut</th>
                                    <th>Date</th>
                                    <th>Actions</th>
                                    <th>Télecharger</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for demande in demandes %}
                                    <tr>
                                        <td>{{ demande.type }}</td>
                                        <td>
                                            {% if demande.statut == 'Validée' %}
                                                <span class="badge bg-success">{{ demande.statut }}</span>
                                            {% elseif demande.statut == 'En attente' %}
                                                <span class="badge bg-warning text-dark">{{ demande.statut }}</span>
                                            {% elseif demande.statut == 'Rejetée' %}
                                                <span class="badge bg-danger">{{ demande.statut }}</span>
                                            {% else %}
                                                <span class="badge bg-secondary">{{ demande.statut }}</span>
                                            {% endif %}
                                        </td>
                                        <td>{{ demande.dateSoumission|date('d/m/Y') }}</td>
                                        <td>
                                            <a href="{{ path('app_demande_show', { idDem: demande.idDem }) }}" class="btn btn-sm btn-primary">Voir</a>
                                        </td>
                                        <td>
                                            {% if demande.statut == 'Validée' %}
                                                <a href="{{ path('admin_demande_pdf', {'idDem': demande.idDem}) }}" 
                                                  class="btn btn- btn-sm" target="_blank">
                                                    📄 Télécharger PDF
                                                </a>
                                            {% endif %}
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        {{ knp_pagination_render(demandes) }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-12">
            <h1>Mes Prêts</h1>
            <div class="card">
                <div class="card-body">
                    {% if prets is empty %}
                        <div class="alert alert-info mt-3">Aucun prêt trouvé.</div>
                    {% else %}
                        <table class="table table-striped table-bordered mt-3">
                            <thead class="bg-primary text-white">
                                <tr>
                                    
                                    <th>Montant</th>
                                    <th>Raison</th>
                                    <th>Date</th>
                                    <th>Statut</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for pret in prets %}
                                    <tr>
                                    
                                        <td>{{ pret.montant }}</td>
                                        <td>{{ pret.raison }}</td>
                                        <td>{{ pret.dateDemande|date('d/m/Y') }}</td>
                                        <td>
                                            {% if pret.statut == 'Validé' %}
                                                <span class="badge bg-success">{{ pret.statut }}</span>
                                            {% elseif pret.statut == 'En attente' %}
                                                <span class="badge bg-warning text-dark">{{ pret.statut }}</span>
                                            {% elseif pret.statut == 'Rejeté' %}
                                                <span class="badge bg-danger">{{ pret.statut }}</span>
                                            {% else %}
                                                <span class="badge bg-secondary">{{ pret.statut }}</span>
                                            {% endif %}
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        {{ knp_pagination_render(prets) }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

     <div class="row mb-4">
        <div class="col-12">
            <h1>Mes Avances</h1>
            <div class="card">
                <div class="card-body">
                    {% if avances is empty %}
                        <div class="alert alert-info mt-3">Aucune avnance trouvé.</div>
                    {% else %}
                        <table class="table table-striped table-bordered mt-3">
                            <thead class="bg-primary text-white">
                                <tr>
                                    
                                    <th>Montant</th>
                                    <th>Raison</th>
                                    <th>Date</th>
                                    <th>Statut</th>
                                </tr>
                            </thead>
                            <tbody>
                                {% for avance in avances %}
                                    <tr>
                                        
                                        <td>{{ avance.montant }}</td>
                                        <td>{{ avance.raison }}</td>
                                        <td>{{ avance.date_demande|date('d/m/Y') }}</td>
                                        <td>
                                            {% if avance.statut == 'Validée' %}
                                                <span class="badge bg-success">{{ avance.statut }}</span>
                                            {% elseif avance.statut == 'En attente' %}
                                                <span class="badge bg-warning text-dark">{{ avance.statut }}</span>
                                            {% elseif avance.statut == 'Rejeté' %}
                                                <span class="badge bg-danger">{{ avance.statut }}</span>
                                            {% else %}
                                                <span class="badge bg-secondary">{{ avance.statut }}</span>
                                            {% endif %}
                                        </td>
                                    </tr>
                                {% endfor %}
                            </tbody>
                        </table>
                        {{ knp_pagination_render(avances) }}
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
